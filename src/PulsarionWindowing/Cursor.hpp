#pragma once
// Everything about cursors, such as changing cursor image, hiding cursor, etc.

namespace Pulsarion::Windowing
{

    enum class CursorMode
    {
        Normal,
        Hidden,
        Captured,
    };
}
